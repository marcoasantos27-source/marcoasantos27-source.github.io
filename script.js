const menuToggle=document.getElementById("menuToggle");const navLinks=document.getElementById("navLinks");menuToggle.addEventListener("click",()=>{const isOpen=navLinks.classList.toggle("active");menuToggle.setAttribute("aria-expanded",isOpen)});document.querySelectorAll(".nav-links a").forEach(link=>{link.addEventListener("click",()=>{navLinks.classList.remove("active");menuToggle.setAttribute("aria-expanded","false")})});const revealElements=document.querySelectorAll(".reveal");function revealOnScroll(){const windowHeight=window.innerHeight;revealElements.forEach(element=>{const elementTop=element.getBoundingClientRect().top;if(elementTop<windowHeight-80){element.classList.add("active")}})}window.addEventListener("scroll",revealOnScroll);window.addEventListener("load",revealOnScroll);const contactForm=document.getElementById("contactForm");const successMessage=document.getElementById("successMessage");function setError(input,message){const formGroup=input.parentElement;const small=formGroup.querySelector(".error-message");formGroup.classList.add("error");small.textContent=message}function clearError(input){const formGroup=input.parentElement;const small=formGroup.querySelector(".error-message");formGroup.classList.remove("error");small.textContent=""}function isValidEmail(email){const emailRegex=/^[^\s@]+@[^\s@]+\.[^\s@]+$/;return emailRegex.test(email)}contactForm.addEventListener("submit",event=>{event.preventDefault();const name=document.getElementById("name");const email=document.getElementById("email");const message=document.getElementById("message");let isValid=true;successMessage.textContent="";if(name.value.trim().length<3){setError(name,"Digite um nome com pelo menos 3 caracteres.");isValid=false}else{clearError(name)}if(!isValidEmail(email.value.trim())){setError(email,"Digite um e-mail válido.");isValid=false}else{clearError(email)}if(message.value.trim().length<10){setError(message,"Digite uma mensagem com pelo menos 10 caracteres.");isValid=false}else{clearError(message)}if(isValid){successMessage.textContent="Mensagem validada com sucesso! Em um projeto real, ela seria enviada.";contactForm.reset()}});